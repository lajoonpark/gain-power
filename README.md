# gain-power

A browser-based incremental strategy prototype called **Gain Power!**.

## Run locally

From the repository root, serve the files with any static server. Example:

```bash
python3 -m http.server 4173
```

Then open <http://localhost:4173>.
